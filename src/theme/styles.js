const styles = {
  global: (props) => {
    console.log('props: ', props);
    return {
      body: {
        bg: '#F6F8FF',
        lineHeight: 'base',
      },
    };
  },
};

export default styles;
