import React, { useContext, useRef, useState } from 'react';
import Layout from '../layouts/layout';

export default function PrivateInfo(articleId) {
  console.log(articleId);
  return <h1>PrivateInfo.js</h1>;
}
